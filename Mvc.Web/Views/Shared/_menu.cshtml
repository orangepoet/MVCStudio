@model IList<Mvc.Web.Models.MenuItem>
<ul>
    @foreach (var menu in Model) {
        <li><a href="#">@menu.MenuName</a>
            <ul>
                @foreach (var child in menu.Children) {
                    <li>
                        @Html.ActionLink(child.MenuName, child.Action, child.Controller)
                    </li>
                }
            </ul>
        </li>
    }
</ul>
